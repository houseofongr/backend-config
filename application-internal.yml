internal:
  gateway:
    base-url: http://${INTERNAL_GATEWAY_ADDRESS}:${INTERNAL_GATEWAY_PORT}
  auth:
    base-url: http://${INTERNAL_AUTH_ADDRESS}:${INTERNAL_AUTH_PORT}
    register-business-user-credential: /auth/register/business-user
  file:
    base-url: http://${INTERNAL_FILE_ADDRESS}:${INTERNAL_FILE_PORT}
    upload-file-url: /files/upload
    get-file-info-url: /files/%s/info
  user:
    base-url: http://${INTERNAL_USER_ADDRESS}:${INTERNAL_USER_PORT}
    get-user-info-url: /users/%s/info
  universe:
    base-url: http://${INTERNAL_UNIVERSE_ADDRESS}:${INTERNAL_UNIVERSE_PORT}